[TOC]

# 模板

模板是预先设定好特定格式、固定文字数据和公式等内容的工作簿。

*快捷键`Ctrl`+`F2`可以调用另存为窗口*

模板可以保存的内容：①文字数值、公式函数、图表、链接等数据操作元素；②自定义的数字格式；③单元格样式；④打印设置；⑤工作表的设置，如显示网格线、显示行号列标等。

Excel 2019的模板文件扩展名为`.xltx`或`.xltm`，其中`.xltx`为普通的模板文件（不能包含宏代码），`.xltm`为包含宏代码的模板文件。